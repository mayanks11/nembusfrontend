/**
 * Auth User Reducers
 */
 import produce from 'immer';

 /**
  * initial auth user
  */
 const INIT_STATE = {
     indexdb = []
 };
 
 export default (state = INIT_STATE, action) => {
     return produce(state, draft => {
         switch (action.type) {
          
         }
     });
 };
 